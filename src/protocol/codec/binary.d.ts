export function toUint8Array(value: Uint8Array | ArrayBuffer | number[]): Uint8Array;
export function concatBytes(...values: Array<Uint8Array | ArrayBuffer | number[]>): Uint8Array;
export function valuesEqual(a: unknown, b: unknown): boolean;
